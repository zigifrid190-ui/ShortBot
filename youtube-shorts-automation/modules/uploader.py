def upload_youtube(video_path: str, thumb_path: str, roteiro: str):
    """
    Stub para upload no YouTube via Data API v3.
    """
    print(f"[*] Iniciando processo de Upload para o YouTube...")
    print(f"  - Vídeo: {video_path}")
    print(f"  - Thumb: {thumb_path}")
    print(f"  - (Falta implementar OAuth completo com a chave client_secret.json)")
    print("[*] Upload concluído (simulado).")
